{
  "name": "slt-js",
  "version": "1.0.0",
  "private": true,
  "description": "SLT-JS Monorepo — Semantic Linguistic Transpiler for natural-language-driven JavaScript construction",
  "license": "MIT",
  "author": "Kandi & ChatGPT",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "packages/*",
    "packages/@slt-lang/*"
  ],
  "scripts": {
    "build": "pnpm -r --parallel build",
    "test": "pnpm -r --parallel test",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "prepare": "husky install"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "eslint": "^9.0.0",
    "prettier": "^3.2.5",
    "husky": "^9.0.7"
  }
}
